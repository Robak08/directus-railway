# Krakovan opas DirectusCMS

Email subject hook

Slugify interface plugin updated for directus v12 and included

push notification bundle extension

directus-extension-krk-tours — tours (kierrokset) schema scaffold, permissions, and seed endpoints
