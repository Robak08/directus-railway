FROM node:22-alpine

# Match Railway Postgres major version (pg_dump must be >= server version).
RUN apk add --no-cache postgresql17-client

WORKDIR /app

COPY scripts/backup/package.json scripts/backup/package-lock.json ./
RUN npm ci --omit=dev

COPY scripts/backup/src ./src

CMD ["node", "src/index.js"]
